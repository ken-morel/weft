# implement this
# it should support parsing a version spec
# finding if a version matches the spec
# or else getting the specs default install version in case
# the already installed version doesn't match th specs